@using Microsoft.AspNetCore.SignalR;
@{
    ViewData["Title"] = "Index";
}

@*<h1>Index</h1>

<script src="~/microsoft/signalr/dist/browser/signalr.min.js"></script>


@section Scripts{

    <div class="content">
        <div class="container-fluid">
            <div class="row">
                <div class="col-md-12">
                    <div class="card">
                        <script type="text/javascript">
                            $(document).ready(() => { //Hazır olduğunda
                                var conn = new signalR.HubConnectionBuilder().withUrl("https://localhost:7052/SignalRHub").build();  //Buraya istekte bulun
                                //Bağlantı durumunu yazmak için
                                $("connstatus").text(conn.state);

                                conn.start().then(() => {
                                    $("#connstatus").text(conn.state);   //Bağlantının durumunu bu sefer bağlandıktan sonra yaz
                                }).catch((err) => { console.log(err) }); //Eğer bi hata oluşursa logda yazdır.
                            })
                        </script>
                    </div>
                </div>
            </div>
        </div>
    </div>

}

    <div class="row">
        <div class="col-md-8 offset-2">
            <div class="alert alert-success">Bağlantının Durumu: <strong id="connstatus"></strong></div>
        </div>
    </div>*@


<h1>Index</h1>
 
@section Scripts
{
<script src="~/microsoft/signalr/dist/browser/signalr.min.js"></script>

    <div class="content">
        <div class="container-fluid">
            <div class="row">
                <div class="col-md-12">
                    <div class="card">
                        <script type="text/javascript">
                            $(document).ready(() => {
                                var conn = new signalR.HubConnectionBuilder().withUrl("https://localhost:7052/SignalRHub").build();
                                $("#connstatus").text(conn.state);
 
                                conn.start().then(() => {
                                    $("#connstatus").text(conn.state);
                                }).catch((err) => { console.log(err) });
                            });
                        </script>
                    </div>
                </div>
            </div>
        </div>
    </div>
}
 
<div class="row">
    <div class="col-md-8 offset-2">
        <div class="alert alert-success">Bağlantının Durumu:<strong id="connstatus"></strong></div>
    </div>
</div>